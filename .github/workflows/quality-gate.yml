name: quality-gate

on:
  # allow manual triggering of action
  workflow_dispatch:

  # Allow other workflows to invoke this workflow
  workflow_call:

  pull_request:
    branches:
      - "main"

  push:
    tags:
      - '*'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.10"

    - name: Install Molecule
      run: |
        pip install -U pip setuptools wheel
        pip install ansible ansible-lint molecule molecule-plugins[docker]

    - name: Test install latest
      run: molecule  test --scenario-name install-latest

    - name: Test specific install version
      run: molecule test --scenario-name install-version

    - name: Test repository local signing
      run: molecule test --scenario-name repository-local-signing

    - name: Test removal
      run: molecule test --scenario-name remove

    - name: Test compositions
      run: molecule test --scenario-name compositions

---
- name: Converge
  hosts: all
  become: true
  gather_facts: true
  roles:
  - role: hth.docker
    vars:
      docker:
        version: "5:29.6.0-1~ubuntu.24.04~noble"
        priority: true
        compose:
          remove: false
